# F6-04 — Hash, SemVer y diff entre versiones

## Hash del dataset

Dataset validado:

`data/validated/coco.json`

Hash DVC/MD5:

`457cc9fcf36cd8699b21f292ebbe49cc`

El remote DEV fue sincronizado y verificado mediante:

```bash
python -m dvc push -r dev
python -m dvc status -c -r dev
```
